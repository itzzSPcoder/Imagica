kind = "web"
previewPath = "/"
title = "Sketch to Component"
version = "1.0.0"
id = "artifacts/sketch-to-component"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25383

[services.development]
run = "pnpm --filter @workspace/sketch-to-component run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/sketch-to-component", "run", "build" ]
publicDir = "artifacts/sketch-to-component/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25383"
BASE_PATH = "/"
